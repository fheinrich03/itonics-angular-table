import { Routes } from '@angular/router'
import { StarshipsPage } from './pages/starhips-page/starships-page'

export const routes: Routes = [
    {
        path: '',
        component: StarshipsPage,
    },
]
